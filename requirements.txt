# Build Tools (Required for Python 3.12+ where distutils was removed)
setuptools>=65.0.0
wheel>=0.40.0

# FastAPI Core
# Note: If fastapi>=0.100.0 is already installed, it may require pydantic>=2.0
# Adjust these constraints based on your installed FastAPI version
fastapi>=0.100.0
uvicorn[standard]>=0.23.0
python-multipart>=0.0.6
# Using flexible constraints - will work with pydantic 1.x or 2.x depending on FastAPI version
pydantic>=1.10.0
pydantic-settings>=1.10.0

# Database
sqlalchemy>=1.4.0,<2.0.0
alembic>=1.10.0,<2.0.0
psycopg2-binary>=2.9.0
pgvector>=0.2.0

# AI/ML Core Libraries (Python 3.12 compatible)
# Note: Updated versions for Python 3.12 compatibility
# NumPy 1.26+ has Python 3.12 wheels and maintains API compatibility
numpy>=1.26.0,<2.0.0
pandas>=2.0.0,<3.0.0
scikit-learn>=1.3.0,<2.0.0
scipy>=1.11.0,<2.0.0

# Deep Learning (Python 3.12 compatible)
# Note: TensorFlow 2.16+ required for Python 3.12 support
# tf-keras is required for Transformers compatibility with Keras 3
tensorflow>=2.16.0
tf-keras>=2.20.0
torch>=2.0.0
torchvision>=0.15.0
transformers>=4.38.0
sentence-transformers>=2.3.0

# Computer Vision (Python 3.12 compatible)
opencv-python>=4.10.0
Pillow>=10.0.0
imageio>=2.34.0

# Medical Document Processing
PyPDF2>=3.0.0
pdf2image>=1.17.0
pytesseract>=0.3.10
pydicom>=2.4.0  # For DICOM medical image format support

# NLP (Python 3.12 compatible)
spacy>=3.8.0
nltk>=3.9.0
textblob>=0.18.0

# Time Series & Forecasting (Python 3.12 compatible)
# Note: statsmodels 0.14.6 is latest available version (0.15.0 doesn't exist yet)
statsmodels>=0.14.0
prophet>=1.2.0
pmdarima>=2.1.0

# Recommendation Systems (Python 3.12 compatible)
# Note: scikit-surprise 1.1.4 is latest available version (1.2.0 doesn't exist yet)
implicit>=0.7.0
scikit-surprise>=1.1.0

# Feature Engineering (Python 3.12 compatible)
category-encoders>=2.7.0
feature-engine>=1.7.0

# Model Training & Evaluation (Python 3.12 compatible)
# Note: Some packages may not have newer versions yet - using latest available
xgboost>=2.0.0
lightgbm>=4.0.0
catboost>=1.2.0  # 1.2.8 is latest available (1.3.0 doesn't exist yet)
imbalanced-learn>=0.11.0

# Vector Databases & Embeddings (Python 3.12 compatible)
faiss-cpu>=1.8.0
chromadb>=0.5.0

# API & HTTP
httpx>=0.24.0,<0.27.0
aiohttp>=3.8.0,<4.0.0
requests>=2.28.0,<3.0.0

# Utilities
python-dotenv>=0.19.0,<1.1.0
python-jose[cryptography]>=3.3.0,<3.4.0
passlib[bcrypt]>=1.7.0,<1.8.0
python-dateutil>=2.8.0,<3.0.0
pytz>=2022.0

# Monitoring & Logging
prometheus-client>=0.16.0,<0.20.0
structlog>=22.0.0,<25.0.0

# Testing
pytest>=7.0.0,<8.0.0
pytest-asyncio>=0.20.0,<0.24.0

# CORS (Note: python-cors doesn't exist, using fastapi's built-in CORS)
# CORS is handled by fastapi.middleware.cors.CORSMiddleware

